# Local development uses the Vite proxy to http://localhost:5000.
# For a separate API host, use its full address including /api.
# Railway frontend variable (set before building):
# VITE_API_URL=https://career-os-server-production.up.railway.app/api
VITE_API_URL=/api
